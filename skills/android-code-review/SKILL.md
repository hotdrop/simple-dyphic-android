---
name: android-code-review
description: このアプリの Android 実装をレビューするスキル。Compose UI、ViewModel/Repository 設計、Coroutine/Flow、Firebase 連携、テスト不足、回帰リスクを優先度付きで検出するときに使う。
---

# Android Code Review Skill

## レビュー手順
1. 変更ファイルを機能単位で把握する。
2. 仕様同等性を確認し、不要な仕様追加や挙動変更を検出する。
3. UI、状態管理、非同期、データ同期、テストの順に確認する。
4. 重大度順で指摘を整理する。

## 観点チェックリスト
- Compose:
  - Route/Screen 分離が守られているか。
  - 条件分岐 UI の Preview が揃っているか。
  - Preview が通常/ローディング/エラー/空状態 + 画面固有状態を網羅しているか。
  - 文字列・色・寸法の直書きがないか。
- ViewModel / Repository:
  - 層責務が混在していないか。
  - `UiState` とイベントが明示されているか。
  - Hilt 以外の DI が混入していないか。
  - Room 変更時に `version` 更新・Migration・Migration テストが揃っているか。
  - Health Connect 指標で「0実績」と「未取得」が分離されているか。
- Coroutines / Flow:
  - キャンセル安全性があるか。
  - 競合ジョブのガードがあるか。
  - 例外が握りつぶされていないか。
- Firebase:
  - 認証状態に応じた分岐が正しいか。
  - backup/restore の整合性が壊れていないか。
- テスト:
  - 主要導線の UI テストが維持されているか。
  - 状態遷移のユニットテストが不足していないか。

## 指摘の出し方
- 事実ベースで書く。推測は推測と明示する。
- `file:line` を付けて再現可能な形で示す。
- 期待挙動と現挙動の差分を1段落で説明する。
- 重大度を `P0` から `P3` で付与する。

## 完了チェック
- 重大度順に整理された指摘一覧がある。
- 各指摘に根拠ファイル位置がある。
- テスト不足や残リスクを明示している。
